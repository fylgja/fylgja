name: Publish Fylgja Tokens

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    if: "contains(github.event.head_commit.message, 'Release tokens')"
    uses: ./.github/workflows/test.yml
  npm-publish:
    needs: test
    if: "contains(github.event.head_commit.message, 'Release tokens')"
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: ./tokens
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
      - run: npm ci
      - run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ""
