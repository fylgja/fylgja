min-release-age=1
allow-git=none
# lightningcss-cli requires its postinstall script to download the platform binary.
# Without this, builds fail with "command not found" after npm ci.
# TODO: replace with `npm approve-scripts lightningcss-cli` once npm ships that command.
ignore-scripts=false
