{
  "name": "@fylgja/base",
  "version": "2.0.0",
  "description": "",
  "homepage": "https://fylgja.dev/",
  "author": "Fylgja",
  "license": "MIT",
  "main": "_index.scss",
  "unpkg": "base.css",
  "style": "base.css",
  "repository": {
    "type": "git",
    "url": "https://github.com/fylgja/fylgja",
    "directory": "base"
  },
  "bugs": "https://github.com/fylgja/fylgja/issues",
  "keywords": [],
  "scripts": {
    "start": "npx sass --no-source-map -w src:./",
    "build:scss": "npx sass --no-source-map src:./",
    "build:preflight": "npx lightningcss --minify --bundle preflight.css -o preflight.min.css",
    "build:btn": "npx lightningcss --minify --bundle btn.css -o btn.min.css",
    "build:form": "npx lightningcss --minify --bundle form.css -o form.min.css",
    "build:min": "npm run build:preflight && npm run build:btn && npm run build:form",
    "prerelease": "npm run build:scss && npm run build:min && npm run test",
    "release": ""
  },
  "devDependencies": {
    "@fylgja/stylelint-config": "^7.0.0",
    "lightningcss-cli": "^1.24.1",
    "sass": "^1.75.0"
  },
  "stylelint": {
    "extends": "@fylgja/stylelint-config"
  }
}
