{
  "name": "@fylgja/form",
  "version": "1.0.8",
  "description": "The form component is super easy to use. Since it is a zero config module that styles the form elements directly",
  "homepage": "https://fylgja.dev/",
  "author": "Fylgja",
  "license": "MIT",
  "main": "_index.scss",
  "unpkg": "form.css",
  "style": "form.css",
  "repository": {
    "type": "git",
    "url": "https://github.com/fylgja/fylgja",
    "directory": "components/form"
  },
  "bugs": "https://github.com/fylgja/fylgja/issues",
  "keywords": [
    "fylgja",
    "fylgja component",
    "form"
  ],
  "scripts": {
    "build:field": "npx sass style-field.scss style-field.css --no-source-map -s compressed -I node_modules",
    "build:box": "npx sass style-box.scss style-box.css --no-source-map -s compressed -I node_modules",
    "build:default": "npx sass form.scss form.css --no-source-map -s compressed -I node_modules",
    "build": "npm run build:field && npm run build:box && npm run build:default"
  },
  "dependencies": {
    "@fylgja/theme": "^1.0.0"
  }
}
